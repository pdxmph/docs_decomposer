:javascript
  $(document).ready(function() {
        $('table').filterTable({ // apply filterTable to all tables on this page
            inputSelector: '#filter-table' // use the existing input instead of creating a new one
        });
    });
