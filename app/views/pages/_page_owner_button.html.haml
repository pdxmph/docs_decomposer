-if current_user.try(:admin?) && @page.user_id == nil
  =link_to pages_set_page_owner_path(:page_id => @page.id, :user_id => current_user.id), :remote => true, :method => :post do
    .btn.btn-primary.btn-xs
      Own This Page
- elsif  current_user.try(:admin?) && @page.user_id == current_user.id
  =link_to pages_set_page_owner_path(:page_id => @page.id, :user_id => current_user.id, :action_id => "disown"), :remote => true, :method => :post do
    .btn.btn-danger.btn-xs
      Disown This Page
